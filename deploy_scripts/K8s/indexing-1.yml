apiVersion: v1
kind: Service
metadata:
  name: indexing-1 
spec:
  selector:
    app: indexing-1 
  ports:
  - protocol: "TCP"
    port: 5000
    targetPort: 5000
  type: LoadBalancer

---
apiVersion: apps/v1 
kind: Deployment 
metadata:
  name: indexing-1 
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: indexing-1 
  template:
    metadata:
      labels:
        app: indexing-1 
    spec:
      containers:
      - name: indexing-1 
        image: zozonguyen1/indexing:latest
        imagePullPolicy: IfNotPresent 
        ports:
        - containerPort: 5000
        env:
        - name: TOTAL_NUM_INDEXES 
          value: "2"
        - name: CURRENT_INDEX 
          value: "1"
        - name: ML_MODEL 
          value: "resnet" 
